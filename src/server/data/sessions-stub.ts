import * as SessionConstants from '../../test-constants/session-constants';
import { Session } from 'src/app/shared/models/session.model';

export const sessions: Session[] = SessionConstants.sessions;